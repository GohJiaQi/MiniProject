<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Home</title>
    <link rel="icon" type="image/x-icon" href="/FFwebicon.webp">
</head>
<body>
  <%- include('partials/navbar.ejs') %>

  <div class="container" id="myUL">
    <div class="row row-cols-1 row-cols-md-3">
      <% for (let i = 0; i < stalls.length; i++) { %>
        <div class="col mt-4 card-item">
          <div class="card" style="width: 18rem;">
            <img src="/<%= stalls[i].picture %>" class="card-img-top" alt="<%= stalls[i].stallName %>">
            <div class="card-body">
              <h5 class="card-title"><%= stalls[i].stallName %></h5>
              <p class="card-text"><small class="text-muted"><a href="/stalls/<%= stalls[i].stallId %>/update">Last Updated: </a><%= stalls[i].currenttime %></small></p>
              <div class="btn-group">
                <a href="/stalls/<%= stalls[i].stallId %>" class="btn btn-sm btn-outline-primary">See More</a>
                <a href="/stalls/<%= stalls[i].stallId %>/delete" class="btn btn-sm btn-outline-danger" onclick="return confirm('Are you sure you want to delete this stall?')">Delete</a>
              </div>
            </div>
          </div>
        </div>
        <% } %>
      </div>
    </div>
      
</body>
</html>